[project]
name = "fsa-reporter"
version = "1.0.0"

dependencies = [
    "flask",
    "pypdf",
    "flask-sqlalchemy",
]

description = "Assists farmers in filling out a vital form."

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"